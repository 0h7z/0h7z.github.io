#	HTTP response status codes
<script setup lang="ts">
	import { entriesof } from "/main"
	import data from "/[status].json"
</script>

##	1xx: Informational
<p v-once v-for="[k, v] of entriesof(data).filter(([k]) => /^1/.test(k))">
	<a target="_blank" :href="`/${k}/`">{{ k }} {{ v }}</a>
</p>

##	2xx: Success
<p v-once v-for="[k, v] of entriesof(data).filter(([k]) => /^2/.test(k))">
	<a target="_blank" :href="`/${k}/`">{{ k }} {{ v }}</a>
</p>

##	3xx: Redirection
<p v-once v-for="[k, v] of entriesof(data).filter(([k]) => /^3/.test(k))">
	<a target="_blank" :href="`/${k}/`">{{ k }} {{ v }}</a>
</p>

##	4xx: Client Error
<p v-once v-for="[k, v] of entriesof(data).filter(([k]) => /^4/.test(k))">
	<a target="_blank" :href="`/${k}/`">{{ k }} {{ v }}</a>
</p>

##	5xx: Server Error
<p v-once v-for="[k, v] of entriesof(data).filter(([k]) => /^5/.test(k))">
	<a target="_blank" :href="`/${k}/`">{{ k }} {{ v }}</a>
</p>

##	See also
<https://developer.mozilla.org/docs/Web/HTTP/Reference/Status>

