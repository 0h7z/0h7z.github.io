#	HTTP response status codes {# }
<script setup lang="ts">
	import { entriesof } from "./main"
	import data from "./[status].json"
</script>

##	1xx: Informational	{#1xx}
<div v-once v-for="[k, v] of entriesof(data).filter(([k]) => /^1/.test(k))">
	<a :href="`/${k}/`"><p>{{ k }} {{ v }}</p></a>
</div>

##	2xx: Success		{#2xx}
<div v-once v-for="[k, v] of entriesof(data).filter(([k]) => /^2/.test(k))">
	<a :href="`/${k}/`"><p>{{ k }} {{ v }}</p></a>
</div>

##	3xx: Redirection	{#3xx}
<div v-once v-for="[k, v] of entriesof(data).filter(([k]) => /^3/.test(k))">
	<a :href="`/${k}/`"><p>{{ k }} {{ v }}</p></a>
</div>

##	4xx: Client Error	{#4xx}
<div v-once v-for="[k, v] of entriesof(data).filter(([k]) => /^4/.test(k))">
	<a :href="`/${k}/`"><p>{{ k }} {{ v }}</p></a>
</div>

##	5xx: Server Error	{#5xx}
<div v-once v-for="[k, v] of entriesof(data).filter(([k]) => /^5/.test(k))">
	<a :href="`/${k}/`"><p>{{ k }} {{ v }}</p></a>
</div>

##	See also
<https://developer.mozilla.org/docs/Web/HTTP/Reference/Status>

